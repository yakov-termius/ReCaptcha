    import XCTest
    @testable import ReCaptchaPackage

    final class ReCaptchaPackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ReCaptchaPackage().text, "Hello, World!")
        }
    }
